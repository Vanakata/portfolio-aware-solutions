# Copy to `.env` next to docker-compose.yml on the NAS.

# Repo-scoped GitHub PAT the self-hosted runner uses to register against
# Vanakata/portfolio-aware-solutions.
# GitHub → Settings → Developer settings → Personal access tokens.
GITHUB_PAT=
